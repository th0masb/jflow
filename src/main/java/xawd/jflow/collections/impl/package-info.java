/**
 * Implementations of flowing Lists and Sets. There aren't many reasons to
 * access these classes directly (except maybe for the delegating
 * implementations) and all construction should come from the static factories
 * provided in the {@link Lists} and {@link Sets} classes.
 */
package xawd.jflow.collections.impl;